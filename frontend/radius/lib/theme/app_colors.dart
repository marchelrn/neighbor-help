import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromARGB(255, 0, 0, 0);
  static const primaryDark = Color.fromARGB(255, 0, 0, 0);
  static const primarySoft = Color.fromARGB(255, 255, 187, 0);

  static const background = Color(0xFFF9FAFB);
  static const border = Color(0xFFE5E7EB);

  static const textDark = Color(0xFF111827);
  static const textLight = Color(0xFF6B7280);

  static const textDanger = Colors.red;
  static const textDangerBG = Colors.redAccent;
}
